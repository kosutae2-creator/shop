<!DOCTYPE html>
<html lang="sr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ $product->name }} | ProShop</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;700;800&display=swap" rel="stylesheet">
</head>
<body class="bg-[#fcfcfd] font-['Plus_Jakarta_Sans']">

    <nav class="p-6">
        <a href="/" class="text-slate-500 font-bold hover:text-indigo-600 flex items-center gap-2">
            ← Nazad na prodavnicu
        </a>
    </nav>

    <main class="max-w-5xl mx-auto px-4 py-12">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-12 bg-white p-8 rounded-[48px] shadow-sm border border-slate-100">
            
            {{-- Slika proizvoda --}}
            <div class="bg-slate-50 rounded-[32px] overflow-hidden border border-slate-100">
                <img src="{{ asset('storage/' . $product->image) }}" class="w-full h-full object-cover shadow-2xl">
            </div>

            {{-- Informacije --}}
            <div class="flex flex-col justify-center">
                <h1 class="text-4xl font-black italic uppercase tracking-tighter text-slate-900 mb-4">
                    {{ $product->name }}
                </h1>
                
                <p class="text-slate-500 leading-relaxed mb-8 text-lg">
                    {{ $product->description ?? 'Ovaj vrhunski proizvod nudi savršen balans kvaliteta i stila. Idealan izbor za vašu kolekciju.' }}
                </p>

                <div class="text-3xl font-black text-indigo-600 mb-10">
                    {{ number_format($product->price, 0, ',', '.') }} RSD
                </div>

                {{-- Forma za korpu --}}
                <form action="{{ route('cart.store') }}" method="POST" class="space-y-6">
                    @csrf
                    <input type="hidden" name="id" value="{{ $product->id }}">
                    
                    <div class="flex items-center gap-6">
                        <div class="flex flex-col">
                            <span class="text-[10px] font-black uppercase text-slate-400 mb-2 ml-1">Količina</span>
                            <input type="number" name="quantity" value="1" min="1" 
                                class="w-24 bg-slate-100 border-none rounded-2xl py-4 text-center font-black text-slate-900 focus:ring-2 focus:ring-indigo-500">
                        </div>

                        <button type="submit" class="flex-grow bg-indigo-600 hover:bg-indigo-700 text-white font-black py-4 rounded-2xl uppercase tracking-widest transition-all active:scale-95 shadow-xl shadow-indigo-100">
                            Dodaj u korpu
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </main>
</body>
</html>